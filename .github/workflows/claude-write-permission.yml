name: Claude Code Write Permission

on:
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  grant-access:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm write permissions active
        run: echo "Claude Code write permissions are enabled for this repository."
